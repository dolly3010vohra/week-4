import math
result = math.sin(6.2)
from math import sqrt
root = sqrt(49)
from math import *
root = sqrt(49)
val = cos(radians(45))

from math impost pi
print("The value"of pi is", pi)
def findMax(a,b) :
    ***Finds the maximum of two values.***
if ( a > b ) :
    max = a
else:
    max = b
    return max